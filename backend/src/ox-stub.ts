/**
 * Stub for ox WebAuthn modules — used only for TS resolution in Node build.
 * Runtime still uses real ox from node_modules via viem.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const stub: any = {};
export default stub;
