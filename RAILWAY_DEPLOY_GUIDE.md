# 🚂 Гайд: Деплой Backend на Railway

Подробная инструкция по деплою backend сервера Chess Battle на Railway.

## 📋 Предварительные требования

- ✅ Аккаунт на [Railway](https://railway.app)
- ✅ GitHub репозиторий с кодом
- ✅ Railway подключен к GitHub аккаунту

---

## Шаг 1: Подготовка проекта ✅

Проект уже подготовлен:
- ✅ `backend/package.json` обновлен с `postinstall` скриптом
- ✅ `railway.json` создан в корне проекта
- ✅ Build команда включает генерацию Prisma Client

---

## Шаг 2: Создание проекта на Railway

1. **Зайдите на Railway:**
   - Откройте https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите репозиторий `Ivaniiiii/ChessInBse`
   - Railway автоматически создаст сервис

---

## Шаг 3: Настройка сервиса

1. **Откройте созданный сервис** (обычно называется как репозиторий)

2. **Настройте Root Directory:**
   - Перейдите в **Settings** → **Root Directory**
   - Установите: `backend`
   - Это скажет Railway, что нужно деплоить из папки `backend`

3. **Проверьте Build и Start команды:**
   - Build Command: `npm install && npm run build` (или оставьте автоматическое определение)
   - Start Command: `npm start` (или оставьте автоматическое определение)
   - Railway обычно определяет их автоматически

---

## Шаг 4: Добавление PostgreSQL базы данных

1. **В проекте Railway нажмите "+ New"**

2. **Выберите "Database" → "Add PostgreSQL"**

3. **Railway создаст PostgreSQL базу данных автоматически**

4. **Скопируйте DATABASE_URL:**
   - Нажмите на PostgreSQL сервис
   - Перейдите в **"Variables"** или **"Connect"**
   - Скопируйте значение `DATABASE_URL` (Railway создаст его автоматически)

---

## Шаг 5: Подключение DATABASE_URL к сервису

1. **В настройках вашего backend сервиса:**
   - Перейдите в **"Variables"**

2. **Добавьте DATABASE_URL через Reference Variable:**
   - Нажмите **"+ New Variable"**
   - Выберите **"Reference Variable"**
   - Выберите PostgreSQL сервис → `DATABASE_URL`
   - Railway автоматически добавит `DATABASE_URL` в переменные

---

## Шаг 6: Настройка переменных окружения

В настройках вашего сервиса перейдите в **"Variables"** и добавьте:

### Обязательные переменные:

```bash
# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=3001
NODE_ENV=production

# ============================================
# TELEGRAM BOT CONFIGURATION
# ============================================
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ============================================
# GEMINI AI CONFIGURATION
# ============================================
GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# WEB APP CONFIGURATION
# ============================================
WEB_APP_URL=https://your-app.vercel.app

# ============================================
# BASE/ETHEREUM CONFIGURATION
# ============================================
BASE_RPC_URL=https://mainnet.base.org
CHESS_ESCROW_CONTRACT_ADDRESS=0x...
ORACLE_PRIVATE_KEY=0x...
PLATFORM_WALLET_ADDRESS=0x...
```

### Опциональные переменные:

```bash
PLATFORM_COMMISSION=5
TELEGRAM_STARS_API_KEY=
TON_API_KEY=
USDT_WALLET_ADDRESS=
```

**Важно:** 
- `DATABASE_URL` добавляется через Reference Variable (см. Шаг 5)
- Замените все `your_*` значения на реальные
- `WEB_APP_URL` обновите после деплоя frontend на Vercel

---

## Шаг 7: Первый деплой

1. **Railway автоматически начнет деплой** после добавления переменных

2. **Проверьте логи:**
   - Откройте ваш сервис
   - Перейдите в **"Deployments"**
   - Откройте последний деплой
   - Проверьте логи на ошибки

3. **Если есть ошибки:**
   - Проверьте, что все переменные добавлены
   - Убедитесь, что `DATABASE_URL` подключен через Reference Variable
   - Проверьте логи на конкретные ошибки

---

## Шаг 8: Применение миграций базы данных

После успешного деплоя нужно применить миграции Prisma:

### Вариант A: Через Railway CLI (рекомендуется)

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в Railway
railway login

# Подключитесь к проекту
railway link

# Перейдите в директорию backend
cd backend

# Примените миграции
railway run npm run db:push
```

### Вариант B: Через Deploy Hook

1. В Railway создайте Deploy Hook
2. Или временно добавьте в Build Command:
   ```
   npm install && npm run build && npm run db:push
   ```
   (После применения миграций уберите `&& npm run db:push`)

---

## Шаг 9: Настройка домена

1. **В настройках сервиса:**
   - Перейдите в **Settings** → **Networking**

2. **Сгенерируйте домен:**
   - Нажмите **"Generate Domain"**
   - Railway выдаст домен вида: `your-service.up.railway.app`
   - Или добавьте свой Custom Domain

3. **Скопируйте URL:**
   - Скопируйте полный URL (например: `https://chess-backend.up.railway.app`)

---

## Шаг 10: Обновление переменных в Vercel

После успешного деплоя обновите переменные в Vercel:

1. **Зайдите в Vercel Dashboard:**
   - Откройте ваш проект
   - Перейдите в **Settings** → **Environment Variables**

2. **Обновите переменные:**
   ```
   VITE_API_URL=https://your-service.up.railway.app
   VITE_WS_URL=wss://your-service.up.railway.app
   ```

3. **Передеплойте frontend:**
   - Vercel автоматически передеплоит после изменения переменных
   - Или нажмите **"Redeploy"** вручную

---

## Шаг 11: Проверка работы

### Проверка health endpoint:

```bash
curl https://your-service.up.railway.app/health
```

Должен вернуть:
```json
{"status":"ok","timestamp":"2024-01-26T..."}
```

### Проверка WebSocket:

1. Откройте ваше приложение в браузере
2. Откройте консоль разработчика (F12)
3. Проверьте, нет ли ошибок подключения к WebSocket
4. Создайте тестовую игру и проверьте синхронизацию

---

## 🔧 Важные моменты

1. **Root Directory:** Обязательно установите `backend` в настройках сервиса
2. **PORT:** Railway автоматически устанавливает переменную `PORT`, ваше приложение должно использовать `process.env.PORT`
3. **WebSocket:** Railway поддерживает WebSocket из коробки, используйте `wss://` в production
4. **DATABASE_URL:** Используйте Reference Variable для подключения из PostgreSQL сервиса
5. **Миграции:** Применяйте через Railway CLI после первого деплоя

---

## 📝 Чеклист деплоя

- [ ] Репозиторий подключен к Railway
- [ ] Root Directory установлен в `backend`
- [ ] PostgreSQL база данных создана
- [ ] `DATABASE_URL` подключен через Reference Variable
- [ ] Все Environment Variables добавлены
- [ ] Деплой успешен (проверьте логи)
- [ ] Миграции применены (`npm run db:push`)
- [ ] Домен сгенерирован
- [ ] Health endpoint работает
- [ ] Переменные в Vercel обновлены
- [ ] WebSocket работает

---

## 🆘 Решение проблем

### Ошибка "Cannot find module '@prisma/client'"

**Решение:** Убедитесь, что в `package.json` есть скрипт `postinstall`:
```json
"postinstall": "npm run db:generate"
```

### Ошибка подключения к БД

**Решение:** 
- Проверьте, что `DATABASE_URL` подключен через Reference Variable
- Убедитесь, что PostgreSQL сервис запущен
- Проверьте логи на конкретные ошибки подключения

### Ошибка "Port already in use"

**Решение:** Railway автоматически устанавливает `PORT`. Убедитесь, что ваше приложение использует:
```typescript
const PORT = process.env.PORT || 3001;
```

### WebSocket не работает

**Решение:**
- Railway поддерживает WebSocket автоматически
- Убедитесь, что используете `wss://` в production (не `ws://`)
- Проверьте CORS настройки в коде

### Миграции не применяются

**Решение:**
- Используйте Railway CLI: `railway run npm run db:push`
- Или временно добавьте в Build Command (не забудьте убрать после)

---

## 🔄 Обновление приложения

Railway автоматически обновляет приложение при push в GitHub:

1. **Сделайте изменения в коде**
2. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Update backend"
   git push
   ```
3. **Railway автоматически начнет новый деплой**
4. **Проверьте логи в Railway Dashboard**

---

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [Railway PostgreSQL](https://docs.railway.app/databases/postgresql)

---

## ✅ Готово!

После выполнения всех шагов ваш backend будет работать на Railway! Railway автоматически:
- ✅ Перезапускает приложение при падении
- ✅ Обновляет приложение при push в GitHub
- ✅ Предоставляет HTTPS и домен
- ✅ Поддерживает WebSocket
- ✅ Управляет базой данных

Удачи! 🎉
