# 🚂 Настройка Railpack для Backend

Railpack - это новый builder от Railway, который автоматически определяет и собирает ваш проект.

## ✅ Что уже настроено

1. ✅ **Root Directory:** Должен быть установлен в `backend` в Railway Dashboard
2. ✅ **package.json:** Содержит все необходимые скрипты:
   - `build`: `npm run db:generate && tsc`
   - `start`: `node dist/index.js`
   - `postinstall`: `npm run db:generate`
3. ✅ **railway.json:** Обновлен для использования Railpack

## 🔧 Как работает Railpack

Railpack автоматически:
1. Определяет Node.js проект по наличию `package.json`
2. Запускает `npm install`
3. Запускает `npm run build` (если есть скрипт `build`)
4. Запускает `npm start` для запуска приложения

## 📋 Проверка настроек в Railway

### 1. Root Directory
- **Settings** → **Root Directory** = `backend`
- Это критически важно!

### 2. Builder
- **Settings** → **Builder** = `Railpack` ✅ (уже выбрано)

### 3. Build Command (опционально)
- Можно оставить пустым - Railpack определит автоматически
- Или указать явно: `npm install && npm run build`

### 4. Start Command (опционально)
- Можно оставить пустым - Railpack использует `npm start`
- Или указать явно: `npm start`

## 🔍 Что происходит при деплое

1. **Railpack определяет проект:**
   ```
   Detected: Node.js project
   Found package.json in backend/
   ```

2. **Устанавливает зависимости:**
   ```
   npm install
   ```

3. **Запускает postinstall (автоматически):**
   ```
   npm run db:generate
   ```

4. **Собирает проект:**
   ```
   npm run build
   ```
   Это запустит: `npm run db:generate && tsc`

5. **Запускает приложение:**
   ```
   npm start
   ```
   Это запустит: `node dist/index.js`

## ⚠️ Важные моменты

1. **Root Directory обязателен:** Без установки `backend` Railpack будет работать с корнем проекта

2. **DATABASE_URL:** Убедитесь, что подключен через Reference Variable из PostgreSQL сервиса

3. **Переменные окружения:** Все переменные из `backend/env.example` должны быть добавлены в Railway

4. **Миграции:** После первого деплоя примените миграции:
   ```bash
   railway run npm run db:push
   ```

## 🆘 Решение проблем

### Railpack не определяет проект

**Решение:** Проверьте Root Directory - должен быть `backend`

### Ошибка при сборке

**Решение:** Проверьте логи - Railpack покажет конкретную ошибку. Чаще всего это:
- Отсутствующие переменные окружения
- Ошибки в TypeScript коде
- Проблемы с Prisma

### Приложение не запускается

**Решение:** 
- Проверьте логи запуска
- Убедитесь, что `dist/index.js` существует после сборки
- Проверьте переменную `PORT` - Railway устанавливает её автоматически

## ✅ Готово!

После настройки Root Directory и выбора Railpack деплой должен пройти успешно!

Railpack автоматически:
- ✅ Определит Node.js проект
- ✅ Установит зависимости
- ✅ Сгенерирует Prisma Client
- ✅ Соберет TypeScript
- ✅ Запустит приложение

Удачи! 🎉
